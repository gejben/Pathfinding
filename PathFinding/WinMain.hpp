#ifndef WINMAIN_HPP
#define WINMAIN_HPP

#include <Windows.h>
#include <string>

enum screen{
	SCREEN_WIDTH = 500,
	SCREEN_HEIGHT = 500
};

const std::string Title = "PathFiding";

const int TileSize = 10;
const int mapSize = 50;

#endif